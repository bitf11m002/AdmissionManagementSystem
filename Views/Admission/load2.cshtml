@model TermProject2.Models.student

@{
    ViewBag.Title = "load2";
    Layout = null;
}
<script src="~/Scripts/jquery.validate-vsdoc.js"></script>
<script src="~/Scripts/jquery.validate.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>

@using (Html.BeginForm()) {
  
    @Html.ValidationSummary(true)

    
        <div class="editor-label">
            @Html.LabelFor(model => model.Id)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(model => model.Id, new { @Value=@Model.Id})
            @Html.ValidationMessageFor(model => model.Id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.status)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(model => model.status,new { @Value=@Model.status})
            @Html.ValidationMessageFor(model => model.status)
        </div>

        
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    
}

<!--<form id="f1" class="form-horizontal" action="/Admission/index" method="post">
            <div class="control-group">
                <label class="control-label" for="dob"> Reg. No </label>
                <div class="controls">
                    <input type="text" id="n0" placeholder="reg.no" name="id" value="@Model.Id" required>
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="dob">Status</label>
                <div class="controls">
                    <input type="text" id="n1" placeholder="status" name="status" value="@Model.status" required>
                </div>
            </div>
            
            
        </form>-->
